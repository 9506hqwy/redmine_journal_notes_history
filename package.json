{
    "name": "redmine_journal_notes_history",
    "scripts": {
        "check": "biome check --write",
        "format": "biome format --write",
        "lint": "biome lint --write"
    },
    "devDependencies": {
        "@biomejs/biome": "2.3.2",
        "@playwright/test": "^1.53.0",
        "@types/node": "^24.0.1"
    }
}
